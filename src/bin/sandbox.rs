// use moneta::models::*;
// use moneta::schema::*;
// use moneta::{connect, initialize_logging};

// use diesel::prelude::*;
// use log::info;

fn main() {
    // initialize_logging(2);

    // let connection =
    //     connect("postgres://localhost/moneta").expect("error connecting and migrating to db");
}
